<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TekMate Automation - Smart Living Made Simple</title>
    <meta name="description" content="Transform your home, hotel, and workspace with intelligent automation solutions. Bihar's first home automation brand.">
    <meta name="keywords" content="smart home, automation, smart switches, CCTV, Bihar, Patna">
    
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Framer Motion via React CDN -->
    <script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://cdn.skypack.dev/framer-motion@10"></script>
    
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');
        
        * { font-family: 'Inter', sans-serif; scroll-behavior: smooth; }
        .glass { backdrop-filter: blur(20px); }
        .glow-teal { box-shadow: 0 0 30px rgba(17, 103, 98, 0.4); }
        .gradient-text { background: linear-gradient(135deg, #116762 0%, #329b72 50%, #9b7232 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
        .hero-bg { background: linear-gradient(135deg, #0a1a1f 0%, #116762 50%, #329b72 100%); }
        .animate-float { animation: float 6s ease-in-out infinite; }
        @keyframes float { 0%, 100% { transform: translateY(0px); } 50% { transform: translateY(-20px); } }
        @keyframes glow { 0% { box-shadow: 0 0 20px rgba(17,103,98,0.3); } 100% { box-shadow: 0 0 40px rgba(17,103,98,0.6); } }
        .stats-animate { animation: countUp 2s ease-out forwards; }
    </style>
</head>
<body class="bg-gray-50 overflow-x-hidden">

<div id="root"></div>

<script type="text/babel">
const { useState, useEffect, useRef, useCallback } = React;
const { motion, AnimatePresence, useInView, useMotionValue, useAnimation } = window.framerMotion;

// ================================================
// NAVBAR COMPONENT
// ================================================
function Navbar() {
    const [scrolled, setScrolled] = useState(false);
    const [mobileOpen, setMobileOpen] = useState(false);

    useEffect(() => {
        const handleScroll = () => setScrolled(window.scrollY > 50);
        window.addEventListener('scroll', handleScroll);
        return () => window.removeEventListener('scroll', handleScroll);
    }, []);

    return (
        <motion.nav 
            className={`fixed top-0 w-full z-50 transition-all duration-500 ${scrolled ? 'bg-white/95 backdrop-blur-xl shadow-xl' : 'bg-transparent'}`}
            initial={{ y: -100 }}
            animate={{ y: 0 }}
        >
            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div className="flex justify-between items-center py-4">
                    {/* Logo */}
                    <div className="flex items-center space-x-2">
                        <div className="w-12 h-12 bg-gradient-to-r from-[#116762] to-[#329b72] rounded-2xl flex items-center justify-center shadow-2xl">
                            <span className="text-white font-black text-2xl">T</span>
                        </div>
                        <span className="text-2xl font-black gradient-text">TekMate</span>
                    </div>

                    {/* Desktop Menu */}
                    <div className="hidden lg:flex items-center space-x-8">
                        {['Services', 'Products', 'Experience', 'Contact'].map(item => (
                            <a key={item} href={`#${item.toLowerCase()}`} className="text-gray-700 hover:text-[#116762] font-semibold transition-all duration-300 hover:scale-105">{item}</a>
                        ))}
                    </div>

                    {/* CTAs */}
                    <div className="hidden lg:flex items-center space-x-4">
                        <button className="px-6 py-3 text-[#116762] border-2 border-[#116762] rounded-2xl hover:bg-[#116762] hover:text-white transition-all duration-300 font-semibold">
                            Book Consultation
                        </button>
                        <button className="px-8 py-3 bg-gradient-to-r from-[#116762] to-[#329b72] text-white rounded-2xl hover:shadow-glow-teal hover:scale-105 transition-all duration-300 font-semibold shadow-xl">
                            Explore Products
                        </button>
                    </div>

                    {/* Mobile Toggle */}
                    <button 
                        className="lg:hidden p-2"
                        onClick={() => setMobileOpen(!mobileOpen)}
                    >
                        <svg className="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                </div>

                {/* Mobile Menu */}
                <AnimatePresence>
                    {mobileOpen && (
                        <motion.div 
                            initial={{ opacity: 0, height: 0 }}
                            animate={{ opacity: 1, height: 'auto' }}
                            exit={{ opacity: 0, height: 0 }}
                            className="lg:hidden bg-white/95 backdrop-blur-xl border-t border-gray-200 pb-8"
                        >
                            <div className="px-6 pt-6 space-y-4">
                                {['Services', 'Products', 'Experience', 'Contact'].map(item => (
                                    <a key={item} href={`#${item.toLowerCase()}`} className="block py-3 text-xl font-semibold text-gray-700 hover:text-[#116762]" onClick={() => setMobileOpen(false)}>
                                        {item}
                                    </a>
                                ))}
                                <button className="w-full py-4 bg-gradient-to-r from-[#116762] to-[#329b72] text-white rounded-2xl font-bold text-lg mt-4">
                                    Book Consultation
                                </button>
                            </div>
                        </motion.div>
                    )}
                </AnimatePresence>
            </div>
        </motion.nav>
    );
}

// ================================================
// HERO SECTION
// ================================================
function Hero() {
    const features = [
        { name: 'Smart Lock', icon: '🔒' },
        { name: 'Smart Lighting', icon: '💡' },
        { name: 'Smart Switches', icon: '🔄' },
        { name: 'CCTV', icon: '📹' },
        { name: 'Curtains', icon: '🪟' }
    ];

    return (
        <section className="relative h-screen flex items-center justify-center hero-bg overflow-hidden">
            {/* Animated Background Elements */}
            <motion.div 
                className="absolute top-1/4 left-10 w-80 h-80 bg-[#116762]/20 rounded-full blur-3xl"
                animate={{ scale: [1, 1.1, 1], rotate: [0, 180, 360] }}
                transition={{ duration: 20, repeat: Infinity, ease: "linear" }}
            />
            <motion.div 
                className="absolute bottom-1/4 right-10 w-96 h-96 bg-[#329b72]/20 rounded-full blur-3xl"
                animate={{ scale: [1, 1.2, 1], rotate: [0, -180, -360] }}
                transition={{ duration: 25, repeat: Infinity, ease: "linear" }}
            />

            <div className="container mx-auto px-6 text-center relative z-10 max-w-5xl">
                <motion.h1 
                    initial={{ opacity: 0, y: 50 }}
                    animate={{ opacity: 1, y: 0 }}
                    transition={{ duration: 1 }}
                    className="text-6xl md:text-8xl lg:text-9xl font-black mb-8 leading-tight"
                >
                    <span className="text-white block">Smart Living</span>
                    <span className="gradient-text block text-7xl md:text-9xl lg:text-[10rem]">Made Simple</span>
                </motion.h1>

                <motion.p 
                    initial={{ opacity: 0, y: 30 }}
                    animate={{ opacity: 1, y: 0 }}
                    transition={{ duration: 1, delay: 0.3 }}
                    className="text-2xl md:text-3xl text-white/90 mb-12 max-w-3xl mx-auto leading-relaxed"
                >
                    Transform your home, hotel, and workspace with intelligent automation solutions.
                </motion.p>

                <motion.div 
                    initial={{ opacity: 0, y: 30 }}
                    animate={{ opacity: 1, y: 0 }}
                    transition={{ duration: 1, delay: 0.6 }}
                    className="flex flex-col sm:flex-row gap-6 justify-center items-center mb-20"
                >
                    <motion.button 
                        whileHover={{ scale: 1.05 }}
                        whileTap={{ scale: 0.95 }}
                        className="px-12 py-6 bg-white text-[#116762] rounded-3xl font-bold text-xl shadow-2xl hover:shadow-glow-teal transition-all duration-300 border-4 border-white/20 glass"
                    >
                        Book Consultation
                    </motion.button>
                    <motion.button 
                        whileHover={{ scale: 1.05 }}
                        whileTap={{ scale: 0.95 }}
                        className="px-12 py-6 bg-gradient-to-r from-[#116762]/90 to-[#329b72]/90 text-white rounded-3xl font-bold text-xl glass hover:shadow-glow-teal transition-all duration-300 shadow-2xl"
                    >
                        Explore Products
                    </motion.button>
                </motion.div>

                {/* Feature Badges */}
                <div className="flex flex-wrap gap-4 justify-center">
                    {features.map((feature, index) => (
                        <motion.div
                            key={feature.name}
                            initial={{ opacity: 0, y: 30 }}
                            animate={{ opacity: 1, y: 0 }}
                            transition={{ duration: 0.6, delay: 1 + index * 0.1 }}
                            whileHover={{ scale: 1.1, y: -10 }}
                            className="px-8 py-4 bg-white/10 glass rounded-3xl border border-white/30 shadow-2xl hover:shadow-glow-teal transition-all duration-400 flex items-center space-x-4 cursor-pointer"
                        >
                            <span className="text-3xl">{feature.icon}</span>
                            <span className="font-bold text-white text-lg">{feature.name}</span>
                        </motion.div>
                    ))}
                </div>
            </div>

            {/* Scroll Indicator */}
            <motion.div 
                className="absolute bottom-12 left-1/2 transform -translate-x-1/2"
                animate={{ y: [0, 15, 0] }}
                transition={{ duration: 2, repeat: Infinity }}
            >
                <div className="w-6 h-12 border-2 border-white/40 rounded-full flex justify-center items-start p-2">
                    <motion.div 
                        className="w-1 h-4 bg-gradient-to-b from-white/80 to-white/40 rounded-full mt-1"
                        animate={{ y: [0, 20, 0] }}
                        transition={{ duration: 1.5, repeat: Infinity }}
                    />
                </div>
            </motion.div>
        </section>
    );
}

// ================================================
// ABOUT SECTION
// ================================================
function About() {
    const stats = [
        { value: '50+', label: 'Projects', color: '#116762' },
        { value: '2', label: 'Experience Centres', color: '#329b72' },
        { value: '5+', label: 'Years Experience', color: '#9b7232' },
        { value: '100%', label: 'Installation Support', color: '#007f63' }
    ];

    const ref = useRef(null);
    const isInView = useInView(ref, { once: true, amount: 0.3 });

    return (
        <section id="about" className="py-32 px-6 bg-white">
            <div className="max-w-7xl mx-auto">
                <motion.div 
                    ref={ref}
                    initial={{ opacity: 0, y: 60 }}
                    animate={isInView ? { opacity: 1, y: 0 } : {}}
                    transition={{ duration: 0.8 }}
                    className="text-center mb-24"
                >
                    <h2 className="text-6xl md:text-7xl font-black gradient-text mb-8">About TekMate</h2>
                    <p className="text-2xl text-gray-600 max-w-4xl mx-auto leading-relaxed">
                        <strong>Bihar's First Home Automation Brand</strong> since 2019. 50+ successful projects, two live experience centres, expert consultants, affordable smart living.
                    </p>
                </motion.div>

                <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-8 mb-24">
                    {stats.map((stat, index) => (
                        <motion.div
                            key={stat.label}
                            initial={{ opacity: 0, scale: 0.8 }}
                            animate={isInView ? { opacity: 1, scale: 1 } : {}}
                            transition={{ duration: 0.8, delay: index * 0.2 }}
                            whileHover={{ scale: 1.05, y: -10 }}
                            className="group cursor-pointer"
                        >
                            <div className="p-10 rounded-3xl bg-gradient-to-br from-gray-50 to-white shadow-2xl group-hover:shadow-glow-teal transition-all duration-500 border border-gray-100 hover:border-transparent hover:bg-gradient-to-br hover:from-[#116762]/5 hover:to-[#329b72]/5">
                                <div 
                                    className="text-6xl md:text-7xl font-black mb-6"
                                    style={{ background: `linear-gradient(135deg, ${stat.color} 0%, #ffffff 100%)`, WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}
                                >
                                    {stat.value}
                                </div>
                                <div className="text-xl font-bold text-gray-800 group-hover:text-[#116762] transition-colors">{stat.label}</div>
                            </div>
                        </motion.div>
                    ))}
                </div>
            </div>
        </section>
    );
}

// ================================================
// SERVICES SECTION
// ================================================
function Services() {
    const services = [
        { name: 'Smart Switches', desc: 'Modular touch switches for seamless control', icon: '🔄', color: '#116762' },
        { name: 'Smart Locks', desc: 'Advanced security with biometric access', icon: '🔒', color: '#329b72' },
        { name: 'Smart Curtains', desc: 'Automated curtains with voice control', icon: '🪟', color: '#9b7232' },
        { name: 'Entrance Automation', desc: 'Automatic gates and doors', icon: '🚪', color: '#007f63' },
        { name: 'CCTV & Biometrics', desc: '24/7 surveillance systems', icon: '📹', color: '#01464b' },
        { name: 'Sensor Lights', desc: 'Motion-activated smart lighting', icon: '💡', color: '#f4c304' },
        { name: 'Smart Controllers', desc: 'Centralized home automation hub', icon: '🎛️', color: '#f9a204' },
        { name: 'Hotel Automation', desc: 'Complete hotel room automation', icon: '🏨', color: '#116762' },
        { name: 'Video Door Bell', desc: 'Remote door monitoring', icon: '🔔', color: '#329b72' },
        { name: 'Networking', desc: 'Enterprise-grade IT infrastructure', icon: '🌐', color: '#9b7232' }
    ];

    const ref = useRef(null);
    const isInView = useInView(ref, { once: true, amount: 0.3 });

    return (
        <section id="services" className="py-32 px-6 bg-gradient-to-b from-gray-50 to-white">
            <div className="max-w-7xl mx-auto">
                <motion.div 
                    ref={ref}
                    initial={{ opacity: 0, y: 60 }}
                    animate={isInView ? { opacity: 1, y: 0 } : {}}
                    transition={{ duration: 0.8 }}
                    className="text-center mb-24"
                >
                    <h2 className="text-6xl md:text-7xl font-black gradient-text mb-8">Our Services</h2>
                    <p className="text-2xl text-gray-600 max-w-3xl mx-auto">Premium automation solutions designed for modern living</p>
                </motion.div>

                <div className="grid md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
                    {services.map((service, index) => (
                        <motion.div
                            key={service.name}
                            initial={{ opacity: 0, y: 50 }}
                            animate={isInView ? { opacity: 1, y: 0 } : {}}
                            transition={{ duration: 0.6, delay: index * 0.05 }}
                            whileHover={{ y: -15, scale: 1.02 }}
                            className="group cursor-pointer"
                        >
                            <div className="p-10 rounded-3xl bg-white shadow-xl group-hover:shadow-2xl hover:shadow-glow-teal transition-all duration-500 border border-gray-100 hover:border-[#116762]/30 hover:bg-gradient-to-br hover:from-[#116762]/5 hover:to-[#329b72]/5">
                                <div 
                                    className="text-5xl mb-8 group-hover:scale-110 transition-transform duration-500 mx-auto w-20 h-20 rounded-2xl flex items-center justify-center"
                                    style={{ background: `linear-gradient(135deg, ${service.color}20 0%, ${service.color}40 100%)` }}
                                >
                                    {service.icon}
                                </div>
                                <h3 className="text-2xl font-bold text-gray-900 mb-6 group-hover:text-[#116762] transition-colors">{service.name}</h3>
                                <p className="text-gray-600 mb-8 leading-relaxed">{service.desc}</p>
                                <button className="text-[#116762] font-bold hover:text-[#329b72] transition-colors group-hover:underline flex items-center">
                                    Learn More <span className="ml-2">→</span>
                                </button>
                            </div>
                        </motion.div>
                    ))}
                </div>
            </div>
        </section>
    );
}

// ================================================
// PRODUCTS SECTION
// ================================================
function Products() {
    const products = [
        { name: 'Modular Touch Switches', img: '🔌', desc: 'Customizable switch panels' },
        { name: 'Smart Door Locks', img: '🔒', desc: 'Biometric fingerprint access' },
        { name: 'Curtain Motors', img: '🪟', desc: 'Voice controlled automation' },
        { name: 'CCTV Systems', img: '📹', desc: '4K AI surveillance' },
        { name: 'Hotel Locks', img: '🏨', desc: 'RFID card access systems' },
        { name: 'Smart Lighting', img: '💡', desc: 'Scene-based lighting control' }
    ];

    const ref = useRef(null);
    const isInView = useInView(ref, { once: true, amount: 0.3 });

    return (
        <section id="products" className="py-32 px-6 bg-white">
            <div className="max-w-7xl mx-auto">
                <motion.div 
                    ref={ref}
                    initial={{ opacity: 0, y: 60 }}
                    animate={isInView ? { opacity: 1, y: 0 } : {}}
                    transition={{ duration: 0.8 }}
                    className="text-center mb-24"
                >
                    <h2 className="text-6xl md:text-7xl font-black gradient-text mb-8">Product Showcase</h2>
                    <p className="text-2xl text-gray-600 max-w-3xl mx-auto">Premium products engineered for the future of smart living</p>
                </motion.div>

                <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
                    {products.map((product, index) => (
                        <motion.div
                            key={product.name}
                            initial={{ opacity: 0, y: 50 }}
                            animate={isInView ? { opacity: 1, y: 0 } : {}}
                            transition={{ duration: 0.6, delay: index * 0.1 }}
                            whileHover={{ y: -20, scale: 1.02 }}
                            className="group cursor-pointer overflow-hidden rounded-3xl bg-gradient-to-br from-gray-50 to-white shadow-2xl hover:shadow-glow-teal transition-all duration-700 border border-gray-100"
                        >
                            <div className="p-12 text-center">
                                <div className="text-7xl mb-8 group-hover:scale-110 transition-transform duration-500 mx-auto">
                                    {product.img}
                                </div>
                                <h3 className="text-3xl font-black text-gray-900 mb-6 group-hover:text-[#116762] transition-colors">{product.name}</h3>
                                <p className="text-xl text-gray-600 mb-12">{product.desc}</p>
                                <button className="px-8 py-4 bg-gradient-to-r from-[#116762] to-[#329b72] text-white rounded-2xl font-bold hover:shadow-glow-teal hover:scale-105 transition-all duration-300">
                                    View Details
                                </button>
                            </div>
                        </motion.div>
                    ))}
                </div>
            </div>
        </section>
    );
}

// ================================================
// WHY CHOOSE SECTION
// ================================================
function WhyChoose() {
    const reasons = [
        'Affordable Smart Living', 'Professional Installation', 'Expert Support', 
        'Customized Automation', 'Premium Quality', 'Warranty Support', 'Energy Efficient'
    ];

    const ref = useRef(null);
    const isInView = useInView(ref, { once: true, amount: 0.3 });

    return (
        <section id="why-choose" className="py-32 px-6 bg-gradient-to-b from-[#efefef] to-white">
            <div className="max-w-7xl mx-auto">
                <motion.div 
                    ref={ref}
                    initial={{ opacity: 0, y: 60 }}
                    animate={isInView ? { opacity: 1, y: 0 } : {}}
                    transition={{ duration: 0.8 }}
                    className="text-center mb-24"
                >
                    <h2 className="text-6xl md:text-7xl font-black gradient-text mb-8">Why Choose TekMate?</h2>
                    <p className="text-2xl text-gray-700 max-w-3xl mx-auto">Everything you need for a seamless smart home experience</p>
                </motion.div>

                <div className="grid md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
                    {reasons.map((reason, index) => (
                        <motion.div
                            key={reason}
                            initial={{ opacity: 0, scale: 0.8 }}
                            animate={isInView ? { opacity: 1, scale: 1 } : {}}
                            transition={{ duration: 0.6, delay: index * 0.1 }}
                            whileHover={{ scale: 1.05, y: -10 }}
                            className="p-10 rounded-3xl bg-white/70 glass shadow-xl hover:shadow-glow-teal transition-all duration-500 border border-white/50 hover:border-[#116762]/30"
                        >
                            <div className="w-20 h-20 bg-gradient-to-r from-[#116762] to-[#329b72] rounded-2xl flex items-center justify-center mb-8 shadow-2xl mx-auto">
                                <span className="text-3xl text-white font-bold">✓</span>
                            </div>
                            <h3 className="text-2xl font-bold text-gray-900 mb-4 text-center">{reason}</h3>
                        </motion.div>
                    ))}
                </div>
            </div>
        </section>
    );
}

// ================================================
// EXPERIENCE CENTRES
// ================================================
function ExperienceCentres() {
    const centres = [
        {
            name: 'Exhibition Road Centre',
            address: 'Shop #3, ABC Complex, Near Lemon Tree, Exhibition Road, Patna – 800001',
            phone: '9142350754'
        },
        {
            name: 'Khagaul Road Centre',
            address: 'Ayodhya Villa, Near Patna Tyre Mall, Khagaul Road, Patna – 801503',
            phone: '9939490985'
        }
    ];

    const ref = useRef(null);
    const isInView = useInView(ref, { once: true, amount: 0.3 });

    const handleCall = (phone) => window.open(`tel:${phone}`);
    const handleWhatsApp = (phone) => window.open(`https://wa.me/91${phone}`);

    return (
        <section id="experience" className="py-32 px-6 bg-white">
            <div className="max-w-7xl mx-auto">
                <motion.div 
                    ref={ref}
                    initial={{ opacity: 0, y: 60 }}
                    animate={isInView ? { opacity: 1, y: 0 } : {}}
                    transition={{ duration: 0.8 }}
                    className="text-center mb-24"
                >
                    <h2 className="text-6xl md:text-7xl font-black gradient-text mb-8">Experience Centres</h2>
                    <p className="text-2xl text-gray-600 max-w-3xl mx-auto">Visit our showrooms to experience smart living firsthand</p>
                </motion.div>

                <div className="grid md:grid-cols-2 gap-12">
                    {centres.map((centre, index) => (
                        <motion.div
                            key={centre.name}
                            initial={{ opacity: 0, x: index === 0 ? -50 : 50 }}
                            animate={isInView ? { opacity: 1, x: 0 } : {}}
                            transition={{ duration: 0.8, delay: index * 0.2 }}
                            whileHover={{ scale: 1.02 }}
                            className="p-12 rounded-3xl bg-gradient-to-br from-[#116762]/5 to-[#329b72]/5 border border-[#116762]/20 shadow-2xl hover:shadow-glow-teal transition-all duration-500 glass"
                        >
                            <h3 className="text-3xl font-black gradient-text mb-8">{centre.name}</h3>
                            <p className="text-xl text-gray-700 mb-12 leading-relaxed">{centre.address}</p>
                            <div className="space-y-4">
                                <button 
                                    onClick={() => handleCall(centre.phone)}
                                    className="w-full p-4 bg-gradient-to-r from-green-500 to-green-600 text-white rounded-2xl font-bold hover:shadow-glow-teal hover:scale-105 transition-all duration-300 flex items-center justify-center space-x-3"
                                >
                                    <svg className="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
                                        <path fillRule="evenodd" d="M7.797 14.723l1.289-3.848a.999.999 0 00-.081-.888l-2.23-2.237a.999.999 0 00-.886-.18l-3.845 1.287a.999.999 0 00-.388 1.575l11.268-3.507a.999.999 0 00.696-.309l3.507-11.267a.999.999 0 001.575.388l1.288-3.845a.999.999 0 00-.18-.886l-2.237-2.23a.999.999 0 00-.888-.08l-3.848 1.289a.999.999 0 00-.388 1.575l-3.507 11.268a.999.999 0 00.309.696l11.267 3.507a.999.999 0 00.388 1.575l-3.845 1.288a.999.999 0 00-.086.888l1.289 3.848a.999.999 0 00.696.309l3.507 11.268a.999.999 0 001.575.388l1.288-3.845a.999.999 0 00-.18-.886l-2.237-2.23a.999.999 0 00-.888-.08l-3.848 1.289a.999.999 0 00-.388 1.575l-11.268-3.507a.999.999 0 00-.696-.309z" clipRule="evenodd" />
                                    </svg>
                                    Call Now
                                </button>
                                <button 
                                    onClick={() => handleWhatsApp(centre.phone)}
                                    className="w-full p-4 bg-gradient-to-r from-green-600 to-green-700 text-white rounded-2xl font-bold hover:shadow-glow-teal hover:scale-105 transition-all duration-300 flex items-center justify-center space-x-3"
                                >
                                    <svg className="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
                                        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893A11.821 11.821 0 0020.885 3.488"/>
                                    </svg>
                                    WhatsApp
                                </button>
                            </div>
                        </motion.div>
                    ))}
                </div>
            </div>
        </section>
    );
}

// ================================================
// TESTIMONIALS
// ================================================
function Testimonials() {
    const testimonials = [
        { text: "Transformed our entire hotel with seamless automation. Exceptional service!", name: "Rajesh Kumar, Hotel Owner", rating: 5 },
        { text: "The smart home experience is incredible. Highly recommend TekMate!", name: "Priya Sharma, Homeowner", rating: 5 },
        { text: "Professional installation and 24/7 support. Best decision we made.", name: "Amit Patel, Business Owner", rating: 5 }
    ];

    const [current, setCurrent] = useState(0);
    const ref = useRef(null);
    const isInView = useInView(ref, { once: true, amount: 0.3 });

    useEffect(() => {
        const interval = setInterval(() => {
            setCurrent((prev) => (prev + 1) % testimonials.length);
        }, 5000);
        return () => clearInterval(interval);
    }, []);

    return (
        <section id="testimonials" className="py-32 px-6 bg-gradient-to-b from-white to-gray-50">
            <div className="max-w-4xl mx-auto">
                <motion.div 
                    ref={ref}
                    initial={{ opacity: 0, y: 60 }}
                    animate={isInView ? { opacity: 1, y: 0 } : {}}
                    transition={{ duration: 0.8 }}
                    className="text-center mb-24"
                >
                    <h2 className="text-6xl md:text-7xl font-black gradient-text mb-8">What Our Clients Say</h2>
                    <p className="text-2xl text-gray-600">Trusted by homeowners, hotels, and businesses across Bihar</p>
                </motion.div>

                <div className="relative">
                    <AnimatePresence mode="wait">
                        <motion.div
                            key={current}
                            initial={{ opacity: 0, x: 50, scale: 0.9 }}
                            animate={{ opacity: 1, x: 0, scale: 1 }}
                            exit={{ opacity: 0, x: -50, scale: 0.9 }}
                            transition={{ duration: 0.6 }}
                            className="p-12 rounded-4xl bg-white/40 glass shadow-2xl border border-white/30 backdrop-blur-xl max-w-4xl mx-auto text-center"
                        >
                            <div className="flex justify-center mb-8">
                                {[...Array(5)].map((_, i) => (
                                    <span key={i} className={`text-3xl ${i < testimonials[current].rating ? 'text-[#f4c304]' : 'text-gray-300'}`}>⭐</span>
                                ))}
                            </div>
                            <blockquote className="text-2xl font-semibold text-gray-800 mb-12 italic">"{testimonials[current].text}"</blockquote>
                            <cite className="text-2xl font-bold gradient-text not-italic">— {testimonials[current].name}</cite>
                        </motion.div>
                    </AnimatePresence>
                    
                    <div className="flex justify-center mt-12 space-x-4">
                        {testimonials.map((_, index) => (
                            <button
                                key={index}
                                onClick={() => setCurrent(index)}
                                className={`w-3 h-3 rounded-full transition-all duration-300 ${current === index ? 'w-12 bg-[#116762]' : 'bg-gray-300 hover:bg-gray-400'}`}
                            />
                        ))}
                    </div>
                </div>
            </div>
        </section>
    );
}

// ================================================
// CTA BANNER
// ================================================
function CTABanner() {
    return (
        <section className="py-32 px-6 hero-bg">
            <div className="max-w-4xl mx-auto text-center">
                <motion.h2 
                    initial={{ opacity: 0, scale: 0.8 }}
                    whileInView={{ opacity: 1, scale: 1 }}
                    transition={{ duration: 0.8 }}
                    className="text-6xl md:text-7xl lg:text-8xl font-black text-white mb-8"
                >
                    Experience the Future
                    <span className="block gradient-text text-7xl md:text-8xl lg:text-9xl">of Smart Living</span>
                </motion.h2>
                
                <motion.div 
                    initial={{ opacity: 0, y: 30 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    transition={{ duration: 0.8, delay: 0.3 }}
                    className="flex flex-col lg:flex-row gap-6 justify-center items-center"
                >
                    <motion.button 
                        whileHover={{ scale: 1.05 }}
                        className="px-12 py-6 bg-white text-[#116762] rounded-3xl font-bold text-2xl shadow-2xl hover:shadow-glow-teal transition-all duration-300 border-4 border-white/20 glass"
                    >
                        Schedule Demo
                    </motion.button>
                    <motion.button 
                        whileHover={{ scale: 1.05 }}
                        className="px-12 py-6 bg-gradient-to-r from-[#f4c304] to-[#f9a204] text-[#181b2c] rounded-3xl font-bold text-2xl shadow-2xl hover:shadow-glow-teal transition-all duration-300"
                    >
                        Visit Centre
                    </motion.button>
                </motion.div>
            </div>
        </section>
    );
}

// ================================================
// FOOTER
// ================================================
function Footer() {
    return (
        <footer className="bg-gradient-to-r from-[#181b2c] to-[#01464b] py-16 px-6">
            <div className="max-w-7xl mx-auto grid md:grid-cols-4 gap-12">
                {/* Logo & Info */}
                <div className="md:col-span-2">
                    <div className="flex items-center space-x-4 mb-8">
                        <div className="w-16 h-16 bg-gradient-to-r from-[#116762] to-[#329b72] rounded-3xl flex items-center justify-center shadow-2xl">
                            <span className="text-white font-black text-2xl">T</span>
                        </div>
                        <div>
                            <h3 className="text-3xl font-black text-white mb-2">TekMate Automation</h3>
                            <p className="text-gray-300">Bihar's First Home Automation Brand</p>
                        </div>
                    </div>
                    <div className="space-y-4 text-gray-400">
                        <p>www.tekmateautomation.in</p>
                        <div className="flex space-x-4">
                            <a href="tel:9142350754" className="hover:text-[#f4c304] transition-colors">9142350754</a>
                            <a href="tel:9939490985" className="hover:text-[#f4c304] transition-colors">9939490985</a>
                        </div>
                    </div>
                </div>

                {/* Quick Links */}
                <div>
                    <h4 className="text-xl font-bold text-white mb-6">Quick Links</h4>
                    <ul className="space-y-3">
                        {['Home', 'Services', 'Products', 'Experience Centres', 'Contact'].map(item => (
                            <li key={item}><a href={`#${item.toLowerCase().replace(' ', '-')}`} className="text-gray-400 hover:text-white transition-colors">{item}</a></li>
                        ))}
                    </ul>
                </div>

                {/* Services */}
                <div>
                    <h4 className="text-xl font-bold text-white mb-6">Services</h4>
                    <ul className="space-y-3">
                        {['Smart Home', 'Smart Locks', 'CCTV Systems', 'Hotel Automation', 'Networking'].map(item => (
                            <li key={item}><a href="#services" className="text-gray-400 hover:text-white transition-colors">{item}</a></li>
                        ))}
                    </ul>
                </div>
            </div>

            <div className="border-t border-gray-800 mt-16 pt-12 text-center text-gray-400">
                <p>&copy; 2024 TekMate Automation Pvt. Ltd. All rights reserved. | Patna, Bihar</p>
            </div>
        </footer>
    );
}

// ================================================
// MAIN APP COMPONENT
// ================================================
function App() {
    return (
        <div className="min-h-screen">
            <Navbar />
            <Hero />
            <About />
            <Services />
            <Products />
            <WhyChoose />
            <ExperienceCentres />
            <Testimonials />
            <CTABanner />
            <Footer />
        </div>
    );
}

// ================================================
// RENDER APP
// ================================================
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
</script>

</body>
</html>